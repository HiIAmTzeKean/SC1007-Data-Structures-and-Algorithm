
```
// Creation from Preorder
```

```
// Creation from Postorder
for (i=0;postO[i]!='\0';i++)
{
    if (postO[i]==' ')
        continue
    else if (postO[i]<='9' && postO[i]>='0')
    {
        // Check till end of string

        // Push into stack
    }
    else
    {
        // Opt encountered
        L=
        // Pop out 2 from stack
        // Set Opt as root
        // 
    }
}
```